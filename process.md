# PMIS 운영 전환 프로세스

## 1. 목적

이 문서는 다음 전환 목표를 위한 실행 가이드다.

- 더미(샘플) 데이터 제거
- "현장 등록부터 시작" 가능한 초기 운영 상태 구축
- 운영자가 실제 데이터만 입력하도록 절차 정리

---

## 2. 현재 상태 진단 (2026-02-24 기준)

### 2.1 확인된 사실

- 로그인 강제: `PMIS_REQUIRE_LOGIN=true`
- 기본 실행 포트: `3070`
- 인증 경로: `api/auth/[...nextauth]`
- 더미 주입 경로 제거 완료 (운영 전환 브랜치 기준)

### 2.2 "현장 등록부터 시작" 관점의 현재 제약

- 사이트 생성 API는 구현 완료 (`GET/POST /api/sites`, `GET/PATCH /api/sites/[id]`)
- 사이트 전환 UI는 DB 기반으로 전환 완료 (`src/components/layout/site-switcher.tsx`)
- 사이트 선택 로직은 사용자 권한(siteIds) 기반 선택으로 보강 완료 (`src/lib/site-context.ts`)
- 현장 생성 권한은 `super_admin`만 가능 (`requireRole("super_admin")`)
- 사용자 역할 enum: `super_admin | site_admin | manager | viewer`
- 참고: `role=admin` 값은 사용하지 않음
- 현장 멤버십 역할 enum: `site_admin | manager | viewer`

즉, **사이트 등록 부트스트랩 기능은 반영되었고, 운영 전환 관점에서는 초기 최고관리자 지정 및 기존 더미 데이터 정리가 핵심 잔여 과제**다.

---

## 3. 진행 가능한 작업 목록

| ID | 작업 | 지금 즉시 가능 | 설명 | 산출물 |
|---|---|---|---|---|
| P0-1 | 전환 정책 확정 | 가능 | 운영/개발 DB 분리, 더미 삭제 범위 확정 | 삭제 정책 표 |
| P0-2 | DB 백업 | 가능 | 더미 삭제 전 전체 스냅샷 백업 | 백업 파일/스냅샷 |
| P0-3 | 더미 주입 경로 제거 | 가능 | 운영 브랜치에서 더미 주입 실행 경로 제거 | 정리 커밋 |
| P0-4 | 문서 정리 | 가능 | 운영 문서에서 더미 주입 안내 제거/분리 | 문서 업데이트 |
| P1-1 | 사이트 목록/생성 API 구현 | 필요(개발) | `/api/sites` GET/POST 추가 | API 라우트 |
| P1-2 | 사이트 생성 화면 구현 | 필요(개발) | 관리자용 "현장 등록" 화면 추가 | 화면 라우트 |
| P1-3 | 사이트 전환 DB 연동 | 필요(개발) | 하드코딩 제거, 실제 사이트 목록 사용 | SiteSwitcher 개선 |
| P1-4 | 첫 사용자 부트스트랩 정책 | 필요(개발) | 첫 로그인 사용자 `super_admin` 지정/관리자 승인 플로우 | 인증 정책 코드 |
| P2-1 | 더미 데이터 정리 절차 설계 | 가능(개발 후 실행) | dry-run + 실제 삭제 단계 분리 | 정리 실행 계획서 |
| P2-2 | 운영 데이터 초기화 실행 | 가능(승인 후) | 정리 계획 승인 후 본 실행 | 정리 로그 |
| P3-1 | 초기 운영 세팅 | 가능 | 현장 생성 → 관리자 배정 → 코드 마스터 점검 | 초기 운영 데이터 |
| P3-2 | 오픈 전 점검 | 가능 | 로그인, 권한, 사이트 전환, 핵심 CRUD 확인 | 점검 리포트 |

---

## 4. 권장 실행 순서

1. 전환 범위 확정
2. DB 백업 수행
3. 더미 주입 경로 제거(P0-3, P0-4)
4. 사이트 등록 부트스트랩 기능(P1) 구현
5. 더미 데이터 정리 절차(P2-1) 확정
6. dry-run 검증
7. 더미 데이터 정리 본 실행(P2-2)
8. 운영 초기 데이터 입력(현장/사용자/권한)
9. 핵심 시나리오 점검 후 운영 전환

---

## 5. 상세 설명서

### 5.1 사전 체크 (필수)

- 운영 URL/포트 확인: `http://localhost:3070`
- 인증 설정 확인:
  - `NEXTAUTH_URL`
  - `NEXTAUTH_SECRET`
  - `AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET`
- MongoDB 연결 확인
- 백업 완료 확인

### 5.1-a 더미 주입 정책 (운영)

- 운영 기준에서는 샘플 데이터 주입을 사용하지 않는다.
- 저장소/실행 명령/운영 문서에서 더미 주입 경로를 제거한 상태를 유지한다.
- 운영 데이터는 관리자 기능을 통해 직접 등록한다.

### 5.2 사이트 등록 부트스트랩 구현 항목

아래 4개가 완료되어야 "현장 등록부터 시작"이 가능하다.

1. 사이트 API
   - `GET /api/sites` (목록)
   - `POST /api/sites` (생성)
2. 사이트 등록 UI
   - 관리자 메뉴에 "현장 등록/관리" 화면 추가
3. 사이트 전환 UI DB 연동
   - `SiteSwitcher`의 하드코딩 제거
   - 로그인 사용자 권한 기반 노출
4. 무사이트 상태 처리
   - 사이트가 0개면 안내 + 등록 화면으로 유도

### 5.3 더미 데이터 정리 범위 가이드

정리 범위는 아래 2안 중 택1:

- A안(전체 초기화)
  - 사용자/사이트/멤버십/업무데이터 전부 재구축
  - 장점: 가장 깔끔함
  - 단점: 초기 세팅 공수가 큼
- B안(업무 데이터만 정리)
  - 사용자/사이트/권한 유지, 업무성 컬렉션만 삭제
  - 장점: 전환 속도 빠름
  - 단점: 기존 기준 데이터 품질 영향 가능

권장: **운영 오픈 전이면 A안**, 이미 사용자 운영 중이면 B안.

### 5.4 실행 원칙

- 운영 DB 직접 수동 삭제 금지
- 반드시 사전 검증(dry-run) 후 본 실행
- 실행 전후 건수 비교 리포트 남김
- 실패 시 즉시 백업 복구 절차 수행
- 운영 전환 브랜치에는 더미 주입 명령/문서가 남지 않도록 정리

---

## 6. 최종 완료 기준 (Definition of Done)

- 사이트 0개 상태에서도 등록 화면 진입 가능
- 첫 사이트 생성 후 전 모듈이 해당 사이트 기준으로 정상 동작
- 하드코딩 사이트 전환 제거 완료
- 더미 데이터 정리 완료 로그 보관
- 관리자/매니저/열람자 권한 검증 완료
- 운영 매뉴얼과 Runbook에 전환 절차 반영 완료

---

## 7. 실행 체크리스트

- [ ] 백업 완료
- [x] 더미 주입 경로/명령 제거 완료
- [x] 운영 문서에서 더미 주입 안내 분리/제거 완료
- [ ] 사이트 등록 API/UI 배포 완료
- [ ] SiteSwitcher DB 연동 완료
- [ ] 더미 정리 dry-run 통과
- [ ] 더미 정리 본실행 완료
- [ ] 현장 1건 이상 등록 완료
- [ ] 관리자 계정 권한 확인 완료
- [ ] 핵심 기능 스모크 테스트 통과
- [ ] 운영 전환 승인

---

## 8. 다음 실행 권장안

우선순위는 아래 순서가 안전하다.

1. P0-3/P0-4 실행 (더미 주입 경로 제거 + 문서 정리)
2. P1-1 ~ P1-4 구현 (사이트 등록 부트스트랩)
3. P2-1 구현 (더미 정리 절차)
4. 더미 정리 실행 + 초기 운영 데이터 입력
